---
import type { Locale } from '../i18n/ui';

interface Props {
  locale: Locale;
  label: string;
}

const { locale, label } = Astro.props;
const baseUrl = import.meta.env.BASE_URL;
const href = locale === 'en' ? `${baseUrl}es/` : baseUrl;
---

<a class="lang-switch" href={href}>{label}</a>
